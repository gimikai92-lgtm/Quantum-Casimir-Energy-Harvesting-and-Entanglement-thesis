version: '3.8'
services:
  coherence-test:
    build: .
    image: quantum-coherence-test:latest
    container_name: coherence_test
    command: ["run", "--qubits", "3", "--tests", "10"]
    volumes:
      - ./:/app:ro
    environment:
      - RUST_LOG=info
    tty: true
